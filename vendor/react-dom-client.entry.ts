// Re-export every name react-dom/client exposes (createRoot,
// hydrateRoot). Auto-generated.

// @ts-ignore — CJS module, both default + named exposed at runtime.
import * as Mod from "react-dom/client";
export default (Mod as any).default ?? (Mod as any);
export const createRoot = (Mod as any).createRoot;
export const hydrateRoot = (Mod as any).hydrateRoot;
export const version = (Mod as any).version;
